"use client";
import { Check } from "lucide-react";import { Progress } from "@/components/ui/progress";import { cn } from "@/lib/utils";
export function WizardShell({title,description,steps,current,children}:{title:string;description:string;steps:string[];current:number;children:React.ReactNode}){return <div className="mx-auto max-w-5xl"><div className="mb-6"><h1 className="text-3xl font-black tracking-tight">{title}</h1><p className="mt-2 text-[var(--muted-foreground)]">{description}</p><Progress value={(current+1)/steps.length*100} className="mt-5"/><div className="mt-4 hidden grid-cols-5 gap-2 text-center text-xs md:grid" style={{gridTemplateColumns:`repeat(${steps.length},minmax(0,1fr))`}}>{steps.map((s,i)=><div key={s} className={cn("flex items-center justify-center gap-1 rounded-lg px-2 py-2 font-semibold",i===current?"bg-emerald-500/10 text-emerald-600 dark:text-emerald-300":i<current?"text-green-600":"text-[var(--muted-foreground)]")}>{i<current&&<Check className="size-3.5"/>}{i+1}. {s}</div>)}</div><div className="mt-3 text-xs font-bold text-emerald-500 md:hidden">Step {current+1}/{steps.length}: {steps[current]}</div></div>{children}</div>}
