export function ScoreRing({ value, label, suffix = "" }: { value: number; label: string; suffix?: string }) {
  const radius = 42, circumference = 2 * Math.PI * radius, offset = circumference - (value / 100) * circumference;
  return <div className="grid justify-items-center gap-2"><div className="relative size-28"><svg className="size-28 -rotate-90" viewBox="0 0 100 100"><circle cx="50" cy="50" r={radius} fill="none" stroke="var(--muted)" strokeWidth="9"/><circle cx="50" cy="50" r={radius} fill="none" stroke="url(#scoreGradient)" strokeWidth="9" strokeLinecap="round" strokeDasharray={circumference} strokeDashoffset={offset}/><defs><linearGradient id="scoreGradient"><stop offset="0%" stopColor="#047857"/><stop offset="100%" stopColor="#d97706"/></linearGradient></defs></svg><div className="absolute inset-0 grid place-items-center text-xl font-black">{value}{suffix}</div></div><span className="text-center text-xs font-semibold text-[var(--muted-foreground)]">{label}</span></div>;
}
