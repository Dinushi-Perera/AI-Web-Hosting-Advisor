"use client";import { useRouter } from "next/navigation";import { AlertTriangle } from "lucide-react";import { Button } from "@/components/ui/button";export default function ErrorPage({reset}:{error:Error&{digest?:string};reset:()=>void}){const router=useRouter();return <main className="grid min-h-[70vh] place-items-center p-6 text-center"><div><AlertTriangle className="mx-auto size-14 text-amber-500"/><h1 className="mt-5 text-3xl font-black">Something went wrong on our side.</h1><p className="mt-2 text-[var(--muted-foreground)]">We could not load this view. No internal stack trace is shown.</p><div className="mt-6 flex justify-center gap-2"><Button onClick={reset}>Try Again</Button><Button variant="outline" onClick={()=>router.push('/dashboard')}>Dashboard</Button></div></div></main>}
